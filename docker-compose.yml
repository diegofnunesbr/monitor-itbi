services:
  monitor-itbi:
    build: .
    container_name: monitor-itbi
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - wwebjs_auth:/app/.wwebjs_auth
      - wwebjs_cache:/app/.wwebjs_cache

volumes:
  wwebjs_auth:
  wwebjs_cache:
